# CONTEXT.md — 视频音频转录工具 术语表

## 核心术语

| 术语 | 定义 |
|------|------|
| **分享链接** | 视频平台 App 分享功能生成的短链接，需解析重定向获取实际视频地址 |
| **音频提取** | 使用 yt-dlp 从视频中抽取音频流并转为标准格式（mp3）的过程，音频是中间产物，转录完成后删除 |
| **转录** | 将音频通过 ASR API（MiniMax 或阿里云）转换为文字的过程 |
| **临时音频** | 提取过程中产生的音频文件，转录完成后自动清理 |
| **Provider** | ASR 引擎的具体实现，遵循统一的 Transcriber 协议接口 |
| **长音频包装层** | 共享的分段逻辑层，当音频超过 5 分钟时自动用 ffmpeg 分段，逐段调用 Provider 转录后合并结果 |

## 工具名称

`video-audio-transcriber` — 支持抖音、B站等多平台

## 外部依赖

| 依赖 | 用途 |
|------|------|
| yt-dlp | 从视频链接提取音频（多平台） |
| ffmpeg | yt-dlp 的音频转码后端 |
| MiniMax ASR API | 语音识别引擎（短音频同步） |
| 阿里云 DashScope ASR API | 语音识别引擎（qwen-audio-3.0-asr-flash，短音频同步） |
